services:
  omada-log-tail:
    image: busybox:1.36
    profiles:
      - observability
    command: ["sh", "-c", "tail -F /logs/server.log"]
    volumes:
      - omada-logs:/logs:ro

volumes:
  omada-logs:
    external: true
    name: omada-controller_omada-logs

